<p>Create comment for "<%= @article.title %>" post</p>
<%= form_for [@article, @comment] do |f| %>
	<div class="form-group">
	    <%= f.text_area :content, rows: '5', class: 'form-control' %>
	 </div>
	 <%= f.submit @comment.new_record? ? "Create" : "Update", :class=> "btn btn-primary" %>
<% end %>